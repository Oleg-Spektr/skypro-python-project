# Виджет банковских операций
Проект для фильтрации и сортировки банковских транзакций.

## Функции
- `filter_by_state`: фильтрует данные по статусу (EXECUTED, CANCELED и т.д.).
- `sort_by_date`: сортирует транзакции по дате (свежие сверху по умолчанию).